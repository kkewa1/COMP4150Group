<UserControl x:Class="Program.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Program.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#FF0084FF">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <ListView Grid.Column="0" ItemsSource="{Binding MenuItems}" SelectedItem="{Binding SelectedMenuItem}" Margin="10">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding Name}" FontSize="18" Foreground="#333333" FontWeight="Bold"/>
                        <TextBlock Text="{Binding Description}" Foreground="#555555" TextWrapping="Wrap"/>
                        <TextBlock Text="{Binding Price, StringFormat=C}" Foreground="#006600" FontWeight="Bold"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <StackPanel Grid.Column="1" Margin="10">
            <TextBlock Text="Current Order" FontSize="22" Foreground="White" Margin="0 0 0 10"/>

            <ListView ItemsSource="{Binding OrderItems}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <TextBlock Text="{Binding Name}" FontSize="16"/>
                            <TextBlock Text="{Binding Quantity, StringFormat=Qty: {0}}" FontWeight="Bold"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <StackPanel Margin="10 10 0 0">
                <TextBlock Text="Table ID:" Foreground="White"/>
                <TextBox Text="{Binding SelectedTableID}" />

                <TextBlock Text="Staff ID:" Foreground="White" Margin="0 10 0 0"/>
                <TextBox Text="{Binding SelectedStaffID}" />

                <Button Content="Create Order"
                    Command="{Binding CreateOrderCommand}"
                    Margin="0 20 0 0"
                    Padding="10"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
